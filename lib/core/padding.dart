import 'package:flutter/rendering.dart';

class PagePadding extends EdgeInsets {
  const PagePadding.margin() : super.all(15);
  const PagePadding.low() : super.all(10);

  const PagePadding.veryLow() : super.all(2);
}
